{
    "configurations": [
        {
            "name": "Win32",
            // Primary source: the compile database produced by CMake+Ninja.
            // This gives cpptools exact include paths, defines, and compiler
            // flags for every translation unit — no manual maintenance needed.
            "compileCommands": "${workspaceFolder}/build/compile_commands.json",

            // Fallback includes used for header files opened directly that have
            // no matching TU in compile_commands.json (e.g. boost_multiprecision.hpp).
            // Paths under _deps are populated after "CMake: Configure" runs.
            // libs/*/include covers all Boost libraries that multiprecision depends
            // on transitively (config, type_traits, math, mpl, etc.).
            "includePath": [
                "${workspaceFolder}/include",
                "${workspaceFolder}/include/**",
                "${workspaceFolder}/build/_deps/eigen-src",
                "${workspaceFolder}/build/_deps/boost-src/libs/*/include"
            ],
            "cppStandard": "c++20",
            "intelliSenseMode": "windows-msvc-x64",

            // Let CMake Tools take over IntelliSense configuration once the
            // extension has run "CMake: Configure" from the command palette.
            "configurationProvider": "ms-vscode.cmake-tools"
        }
    ],
    "version": 4
}
